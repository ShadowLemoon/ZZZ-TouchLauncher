# ZZZTouchLauncher 实机验证指引

交付物：`ZZZTouchLauncher/bin/Release/`（ZZZTouchLauncher.exe + ZZZTouchCore.dll + ZZZTouchFilterHook.dll 三件套，须同目录）

## 背景约定

- 游戏只在你启动期间处于触屏模式；启动器注入后立即把配置写回 PC，所以手动开游戏永远是干净的 PC 状态。
- 合成器无日志模式已启用：注入后**不应**生成 `ZZZTouchFilter-<pid>.log`。
- 所有真实游戏操作（启动、触摸、停止）由你本人执行。

## 命令行恢复 PC 配置

启动器异常退出、配置残留为触屏模式时，可运行：

```powershell
.\ZZZTouchLauncher.exe --restore-pc
```

该命令只读取启动器同目录的 `config.json`，将对应 `GENERAL_DATA.bin` 的
`LocalUILayoutPlatform` 写为 `2` 后退出；不会启动游戏，也不会执行注入。
成功退出码为 `0`，读取或写入失败为 `1`，参数无效为 `2`。

## 前置

1. 备份配置（可选，求安心）：
   ```powershell
   Copy-Item "D:\Game\ZZZ_CN\ZenlessZoneZero_Data\Persistent\LocalStorage\GENERAL_DATA.bin" "$env:USERPROFILE\Desktop\GENERAL_DATA.bin.bak"
   ```
2. 确认游戏当前未运行。

## 验证 1：未启动 → 自动触屏启动（核心路径）

1. 运行 `ZZZTouchLauncher.exe`（双击或命令行）
2. 预期输出序列：
   ```
   确保触屏模式...
   启动游戏...
   游戏进程已启动（PID=xxx），等待主窗口并注入...
   注入成功，写回 PC 模式（游戏内存已是触屏）...
   监视游戏进程...
   ```
3. **验证触屏效果**：进入游戏，确认触屏布局生效、方向键可用（无启动器辅助注入时方向键不可用，注入后应可用）
4. **验证无日志**：游戏运行期间检查启动器目录和游戏目录，应**没有**新增 `ZZZTouchFilter-*.log`
5. 正常退出游戏
6. 预期输出：
   ```
   游戏已退出，确保 PC 模式...
   启动器退出。
   ```
7. **验证配置恢复**：运行 `ZZZConfigProbe.exe "D:\Game\ZZZ_CN\ZenlessZoneZero_Data\Persistent\LocalStorage\GENERAL_DATA.bin"`，应为 `LocalUILayoutPlatform=2`（PC）

## 验证 2：已启动触屏游戏 → 接管

1. 先把配置设为触屏：`ZZZConfigManager.exe -m 1 -p "D:\Game\ZZZ_CN"`（游戏未运行状态）
2. 启动游戏进入主界面（此时触屏布局但方向键不可用，因为没有注入）
3. 运行 `ZZZTouchLauncher.exe`
4. 预期输出：
   ```
   游戏已启动且为触屏模式，注入接管...
   [接管] 注入成功，监视游戏进程...
   ```
5. 验证方向键可用
6. 退出游戏，启动器随之退出（不碰配置）

## 验证 3：已启动 PC 模式游戏 → 拒绝

1. 配置为 PC（验证 1 之后默认就是 PC）
2. 启动游戏进入主界面（PC 布局）
3. 运行 `ZZZTouchLauncher.exe`
4. 预期输出：
   ```
   游戏已启动且配置为 PC 模式（LocalUILayoutPlatform=2）。
   不注入、不修改配置，启动器退出。
   ```
5. 确认配置未被修改（探针读仍是 2）

## 验证 4：首次运行记录路径（可选）

删除启动器目录下的 `config.json`，运行启动器：
- 预期提示「未检测到游戏路径」→ 你手动启动游戏 → 启动器自动记录路径到 config.json

## 预期之外的输出 → 反馈

| 输出 | 含义 |
|---|---|
| `注入失败：未找到游戏主窗口` | 游戏启动慢，启动器会自动重试一次 |
| `注入失败：合成器安装失败` | 游戏进程内安装失败（版本/反作弊），反馈现场日志 |
| `注入失败：协议事件创建失败（进程可能已有注入会话，请重启游戏）` | 同进程已注入过一次，重启游戏后再试 |
| `注入失败：打开游戏进程句柄失败（请以管理员权限运行）` | 游戏管理员权限运行时启动器也需管理员 |
| `警告：检测到 N 个游戏进程` | 多开场景，仅处理第一个 |
